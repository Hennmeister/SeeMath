package logic.equations;

public class GraphEquation extends Equation {
    public GraphEquation(int id, int problemId, ExpressionTree tree, boolean isCorrect) {
        super(id, problemId, tree, isCorrect);
    }
}
