package logic.equations;

public class AdditionEquation extends Equation {
    public AdditionEquation(int id, int problemId, ExpressionTree tree, boolean isCorrect) {
        super(id, problemId, tree, isCorrect);
    }
}
