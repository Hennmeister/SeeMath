package logic.equations;

public class MultiplicationEquation extends Equation {
    public MultiplicationEquation(int id, int problemId, ExpressionTree tree, boolean isCorrect) {
        super(id, problemId, tree, isCorrect);
    }
}
