package logic.equations;

public class FractionEquation extends Equation {
    public FractionEquation(int id, int problemId, ExpressionTree tree, boolean isCorrect) {
        super(id, problemId, tree, isCorrect);
    }
}
